server:
  port: 8080

spring:
  datasource:
    url: jdbc:tc:mysql:8.2.0:///sugang-test
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  jpa:
    hibernate:
      ddl-auto: create
      show-sql: true
    properties:
      hibernate:
        format_sql: true
